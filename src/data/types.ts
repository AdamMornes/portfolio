export type NavLink = {
  label: string;
  href: string;
};

export type Meta = {
  title: string;
  description: string;
};

export type Image = {
  alt: string;
  backgroundColor?: string; // tailwind bg color class
  credit?: string;
  src: string;
};
